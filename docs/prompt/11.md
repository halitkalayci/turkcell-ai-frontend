User: We have a runtime browser console error.

Constraints:
- Do NOT add new dependencies
- Do NOT refactor unrelated code
- Minimal diff only

Task:

1) Identify root cause from the FIRST compile error.
2) Propose the smallest fix
3) Provide the corrected code for the affected file only

Here is the developer console output:
placeholder.jpg:1  Failed to load resource: net::ERR_NAME_NOT_RESOLVED
---
GET https://cdn.example.com/products/placeholder.jpg net::ERR_NAME_NOT_RESOLVED

When this error occurs:
Browsing `http://localhost:5173/products`



GitHub Copilot: I'll identify where the placeholder image URL is being used and fix it. 

Searched for regex `cdn.example.com|placeholder.jpg` (`**/frontend/src/**/*.{ts,tsx}`), 2 results

Now let me check how this constant is used: 

Searched for text `DEFAULT_PRODUCT_IMAGE` (`**/frontend/src/**/*.{ts,tsx}`), 4 results

Perfect! The root cause is clear:

## Root Cause
constants.ts defines `DEFAULT_PRODUCT_IMAGE` pointing to a non-existent domain `https://cdn.example.com/products/placeholder.jpg`. When ProductCardV2.tsx uses this constant for products without images, the browser fails to load it.

## Smallest Fix
Replace the invalid URL with a valid placeholder image service (placeholder.com is a reliable public service that returns actual placeholder images). 

Using "Replace String in File"

Read [](file:///c%3A/Users/PC1/Desktop/Projects/Education/React/turkcell-ai/frontend/src/utils/constants.ts#115-115), lines 115 to 122

Using "Replace String in File"


```

```
## Fixed

**Root Cause:** Invalid placeholder image URL (`cdn.example.com` doesn't exist)

**Solution:** Updated constants.ts to use `via.placeholder.com`, a reliable public placeholder service that returns valid images.

The error will be resolved when the page reloads. Products without images will now display a gray placeholder with "No Image" text instead of failing to load.

Made changes.